<div class="container m-5">
  <h2>Hello <%= user_signed_in? ? " there, #{current_user.nickname} #{current_user.emoji}" : ", Sunshine!" %></h2>
  <%= link_to 'Add Contact', new_contact_path, class: "btn" %>
  <%= link_to 'My Contacts', contacts_path, class: "btn" %>
</div>
